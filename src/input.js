import React from "react";


const Input = (props)=>{

    return(
        <React.Fragment>
            <input type={props.type} name={props.name} value={state.value}/>
        </React.Fragment>
    )
}

export default Input